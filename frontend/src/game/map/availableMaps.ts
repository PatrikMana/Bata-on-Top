export type AvailableMap = {
  id: string;
  name: string;
  version: string;
  sectionCount: number;
  sectionWidth: number;
  sectionHeight: number;
};

export { AVAILABLE_MAPS } from './availableMaps.generated';
